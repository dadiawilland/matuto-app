import colors from '../../helpers/colors';

const styles = () => ({
  containerContent: {
    width: '100%',
    maxWidth: 1080,
    minHeight: '100vh',
    paddingInline: 64,
    paddingBlock: 20,
    margin: '0 auto'
  }
});

export default styles;
